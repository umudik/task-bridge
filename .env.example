# Local dev (npm run dev)
PORT=3000

# Optional: SQLite path (default ./task-bridge-data/bridge.db when run via npx)
# DATABASE_PATH=./data/bridge.db

# MCP server (apps/mcp — Cursor / Claude Desktop)
# TASK_BRIDGE_URL=http://localhost:3000
# TASK_BRIDGE_TOKEN=your-bearer-token-from-login
