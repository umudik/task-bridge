services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    environment:
      PORT: "3000"
      BACKEND_API_KEY: ${BACKEND_API_KEY:-dev-key}
      NGROK_INSPECTOR_URL: http://ngrok:4040
      DATABASE_PATH: /app/data/bridge.db
      BRIDGE_PROJECTS_PATH: /app/projects.json
      BRIDGE_TASKS_PATH: /app/worker/bridge-tasks.json
      CURSOR_INBOX_PATH: /app/worker/cursor-inbox.json
      WORKER_REPO_PATH: ${WORKER_REPO_PATH:-}
    volumes:
      - bridge_data:/app/data
      - ./projects.json:/app/projects.json:ro
      - ./worker:/app/worker
    ports:
      - "3001:3000"
    restart: unless-stopped

  ngrok:
    image: ngrok/ngrok:latest
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN:?Set NGROK_AUTHTOKEN in .env}
    command:
      - start
      - --all
      - --config
      - /etc/ngrok.yml
    volumes:
      - ./ngrok.yml:/etc/ngrok.yml:ro
    ports:
      - "4040:4040"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  bridge_data:
