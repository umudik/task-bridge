{
  "name": "task-bridge",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix apps/web run build && node scripts/copy-web.mjs && npm --prefix apps/backend run build",
    "test": "npm --prefix apps/backend run test",
    "lint": "npm --prefix apps/backend run lint && cd apps/mobile && gradlew.bat detekt",
    "docker:up": "docker compose up --build -d --remove-orphans",
    "docker:down": "docker compose down --remove-orphans",
    "docker:publish": "node scripts/docker-publish.mjs",
    "docker:publish:mobile": "node scripts/docker-publish.mjs --mobile",
    "mobile:build": "node scripts/docker-mobile-build.mjs"
  }
}
