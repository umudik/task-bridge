build:
  maxIssues: 0

config:
  validation: true

processors:
  active: true

console-reports:
  active: true

output-reports:
  active: true

style:
  ReturnCount:
    active: true
    max: 3
  MagicNumber:
    active: false

potential-bugs:
  active: true

complexity:
  LongMethod:
    active: true
    threshold: 80
  LongParameterList:
    active: true
    functionThreshold: 8

coroutines:
  active: true
